{
  "name": "nexauren",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
